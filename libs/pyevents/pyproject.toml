[project]
name = "pyevents"
version = "0.1.0"
description = "Shared event envelope, publisher and retry-ladder consumer contract"
requires-python = ">=3.11"
dependencies = ["pika==1.3.2"]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

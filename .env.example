# Optional: enables LLM-based feature extraction (falls back to deterministic rules if unset)
OPENAI_API_KEY=
# Optional extra ingestion sources
ADZUNA_APP_ID=
ADZUNA_APP_KEY=
USAJOBS_API_KEY=
USAJOBS_EMAIL=
